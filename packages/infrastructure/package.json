{
  "name": "@tools/infrastructure",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "init": "terraform init",
    "plan": "terraform plan",
    "apply": "terraform apply -auto-approve",
    "output": "terraform output -json",
    "invalidate-cloudfront": "aws cloudfront create-invalidation --path '/*' --distribution-id $(terraform output -raw frontendDistribution)"
  }
}
